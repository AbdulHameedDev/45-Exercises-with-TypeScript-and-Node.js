"use strict";
// Declare a variable named personName of type string
let personName = "Muhammad Ali Jinnah";
// Declare a variable named quote of type string and use template literals to concatenate personName with a quote
let quote = `${personName} once said, "With Faith, Discipline and selfless devotion to duty, there is nothing worthwhile that you cannot achieve."`;
// Output the quote to the console
console.log(quote);
